<?php
/** @var array<string, list<string>> $errors */
/** @var array{identifier:string} $old */
$errors = $errors ?? [];
$old = $old ?? ['identifier' => ''];
?>
<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-lg sm:p-8">
    <h1 class="text-center text-2xl font-bold text-slate-900"><?= h(tr('Forgotten Password')) ?></h1>
    <p class="mx-auto mt-2 max-w-sm text-center text-sm text-slate-600"><?= h(tr('Enter your username or the email you registered with. If we find an account with a password, we will email you a reset link.')) ?></p>
    <form method="post" action="/account/forgotten-password" class="mt-6 space-y-4">
        <div>
            <label class="mb-1 block text-sm font-medium text-slate-700" for="identifier"><?= h(tr('Username or email')) ?></label>
            <input id="identifier" name="identifier" type="text" class="w-full rounded-lg border border-slate-300 px-3 py-2.5 text-slate-900 shadow-sm placeholder:text-slate-400 focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20" placeholder="<?= h(tr('Username or email')) ?>"
                   value="<?= h($old['identifier']) ?>" autofocus autocomplete="username"/>
            <?php if (!empty($errors['identifier'])): ?>
                <div class="mt-1 text-sm text-red-600"><?php foreach ($errors['identifier'] as $msg) {
                    echo '<p>' . h($msg) . '</p>';
                } ?></div>
            <?php endif; ?>
        </div>
        <input type="hidden" name="_csrf" value="<?= h($csrf) ?>"/>
        <button class="w-full rounded-xl bg-gradient-to-r from-sky-600 to-indigo-600 py-3 text-center text-base font-semibold text-white shadow-md shadow-sky-600/20 hover:from-sky-500 hover:to-indigo-500" type="submit"><?= h(tr('Send reset link')) ?></button>
    </form>
    <a class="mt-4 block w-full rounded-xl border border-slate-200 py-3 text-center text-base font-semibold text-slate-700 shadow-sm hover:bg-slate-50" href="/account/login"><?= h(tr('Back to Sign In')) ?></a>
</div>
