<?php
/** @var string $token */
/** @var bool $tokenValid */
/** @var array<string, list<string>> $errors */
/** @var array{password:string,password_confirm:string} $old */
$token = $token ?? '';
$tokenValid = $tokenValid ?? false;
$errors = $errors ?? [];
$old = $old ?? ['password' => '', 'password_confirm' => ''];
?>
<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-lg sm:p-8">
    <h1 class="text-center text-2xl font-bold text-slate-900"><?= h(tr('Reset your password')) ?></h1>

    <?php if (!$tokenValid): ?>
        <p class="mx-auto mt-3 max-w-sm text-center text-sm text-slate-600"><?= h(tr('This reset link is invalid or has expired. Request a new one from the sign-in page.')) ?></p>
        <a class="mt-6 block w-full rounded-xl bg-gradient-to-r from-sky-600 to-indigo-600 py-3 text-center text-base font-semibold text-white shadow-md shadow-sky-600/20 hover:from-sky-500 hover:to-indigo-500" href="/account/forgotten-password"><?= h(tr('Forgotten Password')) ?></a>
        <a class="mt-3 block w-full rounded-xl border border-slate-200 py-3 text-center text-base font-semibold text-slate-700 shadow-sm hover:bg-slate-50" href="/account/login"><?= h(tr('Back to Sign In')) ?></a>
    <?php else: ?>
        <p class="mx-auto mt-2 max-w-sm text-center text-sm text-slate-600"><?= h(tr('Choose a new password for your account.')) ?></p>
        <?php if (!empty($errors['token'])): ?>
            <div class="mt-4 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800"><?php foreach ($errors['token'] as $msg) {
                echo '<p>' . h($msg) . '</p>';
            } ?></div>
        <?php endif; ?>
        <form method="post" action="/account/reset-password" class="mt-6 space-y-4">
            <input type="hidden" name="token" value="<?= h($token) ?>"/>
            <div>
                <label class="mb-1 block text-sm font-medium text-slate-700" for="password"><?= h(tr('New password')) ?></label>
                <input id="password" name="password" type="password" class="w-full rounded-lg border border-slate-300 px-3 py-2.5 text-slate-900 shadow-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20" placeholder="••••••••" autocomplete="new-password"/>
                <?php if (!empty($errors['password'])): ?>
                    <div class="mt-1 text-sm text-red-600"><?php foreach ($errors['password'] as $msg) {
                        echo '<p>' . h($msg) . '</p>';
                    } ?></div>
                <?php endif; ?>
            </div>
            <div>
                <label class="mb-1 block text-sm font-medium text-slate-700" for="password_confirm"><?= h(tr('Confirm new password')) ?></label>
                <input id="password_confirm" name="password_confirm" type="password" class="w-full rounded-lg border border-slate-300 px-3 py-2.5 text-slate-900 shadow-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20" placeholder="••••••••" autocomplete="new-password"/>
                <?php if (!empty($errors['password_confirm'])): ?>
                    <div class="mt-1 text-sm text-red-600"><?php foreach ($errors['password_confirm'] as $msg) {
                        echo '<p>' . h($msg) . '</p>';
                    } ?></div>
                <?php endif; ?>
            </div>
            <input type="hidden" name="_csrf" value="<?= h($csrf) ?>"/>
            <button class="w-full rounded-xl bg-gradient-to-r from-sky-600 to-indigo-600 py-3 text-center text-base font-semibold text-white shadow-md shadow-sky-600/20 hover:from-sky-500 hover:to-indigo-500" type="submit"><?= h(tr('Set new password')) ?></button>
        </form>
        <a class="mt-4 block w-full text-center text-sm font-medium text-brand-600 hover:text-brand-800" href="/account/login"><?= h(tr('Back to Sign In')) ?></a>
    <?php endif; ?>
</div>
