<?php
/** @var array<string, list<string>> $passwordErrors */
$passwordErrors = $passwordErrors ?? [];
?>
<div class="max-w-lg">
    <p class="text-sm font-semibold text-sky-700"><?= h(tr('Settings')) ?></p>
    <h1 class="mt-2 text-2xl font-bold tracking-tight text-slate-900"><?= h(tr('Change password')) ?></h1>
    <p class="mt-2 text-sm text-slate-600"><?= h(tr('Use your current password, then choose a new one.')) ?></p>

    <?php if (!empty($passwordErrors['general'])): ?>
        <div class="mt-4 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800"><?php foreach ($passwordErrors['general'] as $msg) {
            echo '<p>' . h($msg) . '</p>';
        } ?></div>
    <?php endif; ?>

    <form action="/settings/password" method="post" class="mt-8 space-y-4">
        <div class="rounded-xl border border-slate-200 bg-slate-50/80 p-5">
            <div class="space-y-4">
                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700" for="current_password"><?= h(tr('Current password')) ?></label>
                    <input id="current_password" name="current_password" type="password" class="w-full rounded-lg border border-slate-300 px-3 py-2.5 text-slate-900 shadow-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20" autocomplete="current-password"/>
                    <?php if (!empty($passwordErrors['current_password'])): ?>
                        <div class="mt-1 text-sm text-red-600"><?php foreach ($passwordErrors['current_password'] as $msg) {
                            echo '<p>' . h($msg) . '</p>';
                        } ?></div>
                    <?php endif; ?>
                </div>
                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700" for="new_password"><?= h(tr('New password')) ?></label>
                    <input id="new_password" name="new_password" type="password" class="w-full rounded-lg border border-slate-300 px-3 py-2.5 text-slate-900 shadow-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20" autocomplete="new-password"/>
                    <?php if (!empty($passwordErrors['new_password'])): ?>
                        <div class="mt-1 text-sm text-red-600"><?php foreach ($passwordErrors['new_password'] as $msg) {
                            echo '<p>' . h($msg) . '</p>';
                        } ?></div>
                    <?php endif; ?>
                </div>
                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700" for="new_password_confirm"><?= h(tr('Confirm new password')) ?></label>
                    <input id="new_password_confirm" name="new_password_confirm" type="password" class="w-full rounded-lg border border-slate-300 px-3 py-2.5 text-slate-900 shadow-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20" autocomplete="new-password"/>
                    <?php if (!empty($passwordErrors['new_password_confirm'])): ?>
                        <div class="mt-1 text-sm text-red-600"><?php foreach ($passwordErrors['new_password_confirm'] as $msg) {
                            echo '<p>' . h($msg) . '</p>';
                        } ?></div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
        <input type="hidden" name="_csrf" value="<?= h($csrf) ?>"/>
        <button class="w-full rounded-xl bg-gradient-to-r from-sky-600 to-indigo-600 py-3 text-center text-base font-semibold text-white shadow-md shadow-sky-600/20 hover:from-sky-500 hover:to-indigo-500 sm:w-auto sm:px-8" type="submit"><?= h(tr('Change password')) ?></button>
    </form>

    <p class="mt-10">
        <a class="text-sm font-semibold text-brand-600 hover:text-brand-800" href="/settings"><?= h(tr('Back to account settings')) ?></a>
    </p>
</div>
