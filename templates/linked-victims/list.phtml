<?php
/** @var list<array{id:int,username:string,email:string,shared_count:int}> $victims */
?>
<p class="text-sm font-semibold text-sky-700"><?= h(tr('Linked victims')) ?></p>
<h1 class="mt-2 text-3xl font-bold tracking-tight text-slate-900"><?= h(tr('Linked victims')) ?></h1>
<p class="mt-3 max-w-xl text-slate-600"><?= h(tr('A linked victim appears here only when you both saved the exact same line.')) ?></p>

<?php if (empty($victims)): ?>
    <div class="mt-10 rounded-2xl border border-violet-100 bg-gradient-to-br from-violet-50/80 to-sky-50/40 p-6 ring-1 ring-violet-900/[0.04]">
        <p class="font-semibold text-slate-900"><?= h(tr('No linked victims yet')) ?></p>
        <p class="mt-2 text-sm text-slate-600"><?= h(tr('When another victim logs the same line you saved, they will appear as a linked victim here.')) ?></p>
    </div>
    <a href="/messages/detail" class="mt-8 inline-flex rounded-xl bg-gradient-to-r from-sky-600 to-indigo-600 px-5 py-2.5 text-sm font-semibold text-white shadow-md shadow-sky-600/20 hover:from-sky-500 hover:to-indigo-500"><?= h(tr('Share a line')) ?></a>
<?php else: ?>
    <ul class="mt-10 space-y-4">
        <?php foreach ($victims as $v): ?>
            <li class="flex flex-col gap-4 rounded-2xl border border-slate-200/80 bg-white p-5 shadow-md shadow-slate-200/40 sm:flex-row sm:items-center sm:justify-between">
                <div>
                    <p class="font-semibold text-slate-900"><?= h($v['username']) ?></p>
                    <p class="text-sm text-slate-500"><?= h(tr('Shared lines')) ?>: <span class="font-medium text-slate-800"><?= (int) $v['shared_count'] ?></span></p>
                </div>
                <div class="flex flex-wrap gap-2">
                    <a class="rounded-xl border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700 shadow-sm hover:bg-slate-50" href="mailto:<?= h($v['email']) ?>?subject=<?= rawurlencode(tr('Harness — message')) ?>">
                        <?= h(tr('Email')) ?>
                    </a>
                    <a class="rounded-xl bg-gradient-to-r from-sky-600 to-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-md shadow-sky-600/20 hover:from-sky-500 hover:to-indigo-500" href="/linked-victims/<?= h(rawurlencode($v['username'])) ?>">
                        <?= h(tr('View details')) ?>
                    </a>
                </div>
            </li>
        <?php endforeach; ?>
    </ul>
<?php endif; ?>
